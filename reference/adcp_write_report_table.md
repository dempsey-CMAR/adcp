# Writes deployment table for summary report

Writes deployment table for summary report

## Usage

``` r
adcp_write_report_table(metadata)
```

## Arguments

- metadata:

  Deployment metadata from the ADCP TRACKING sheet.

## Value

Returns a data frame with columns for the report table.
