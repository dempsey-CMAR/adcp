# Pivot current data

Pivot current data

## Usage

``` r
adcp_pivot_vars_longer(dat_wide, vars = NULL)
```

## Arguments

- dat_wide:

  Data frame of ADCP current data.

- vars:

  Vector of character strings indicating which columns to pivot. Default
  is all variables.

## Value

Returns data in a long format.
