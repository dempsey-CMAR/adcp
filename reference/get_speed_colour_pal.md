# Generate colour palette for speed values

Generate colour palette for speed values

## Usage

``` r
get_speed_colour_pal(n_colours)
```

## Arguments

- n_colours:

  Integer indicating how many colours to generate.

## Value

Character vector of `n_levels` hex codes from `viridis` option "F".
